packages:
  - package: dbt-labs/dbt_utils
    version: 1.3.0  # Version compatible avec dbt-core >=1.3.0

  - package: calogica/dbt_date
    version: 0.10.1  # Dernière version disponible, compatible avec dbt-core >=1.3.0

  - package: dbt-labs/codegen
    version: 0.13.1  # Dernière version pour remplacer le package déprécié

# `audit-helper` a été retiré, car il n'est pas disponible dans le registre dbt

